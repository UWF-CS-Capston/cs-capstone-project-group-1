{
  "name": "gymbuddy-root",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix Backend && npm install --prefix GymBuddy",
    "dev": "concurrently \"npm run dev --prefix Backend\" \"npm start --prefix GymBuddy\"",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "concurrently": "^8.2.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
